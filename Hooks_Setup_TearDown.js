beforeEach(() => {
    // ···
});

afterEach(() => {
    //···
});



//-----------------------------------------------------------
var speakers;

beforeEach(function () {
    speakers = {
        name: "Kushal"
    };
});

afterEach(function () {
    speakers = {};
});